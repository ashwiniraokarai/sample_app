<!DOCTYPE html>
<html>
  <head>
    <title><%= title_helper yield(:title) %></title>
    <%= render "layouts/rails_default" %>
  </head>
  <%= render "layouts/shim" %>
  
  <body>
    <%= render "/layouts/header" %>
    
    <div class="container">
      <% flash.each do |message_key, message| %>
        <!-- Bootstrap CSS supports styling for four such flash classes for message types: success, info, warning, and danger
        Extract the message-key(type) passed to flash in controller, and use it to instruct the css class (e.g.: alert-success), without needing an ugly if-else type of construct
        -->
        <div class= "alert alert-<%= message_key %>"><%= message %></div>
      <%end%>
      <%= yield %>
    </div>
    
    <%= render "/layouts/footer" %>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>